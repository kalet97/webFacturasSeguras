services:
  front:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: recibo-seguro-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    restart: unless-stopped
